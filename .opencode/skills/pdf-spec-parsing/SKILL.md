---
name: pdf-spec-parsing
description: Парсинг PDF проектной документации (РД) и проверка ВОР/спецификаций. Use when extracting spec tables, quantities, or texts from РД PDFs (ГОСТ 21.110 specs, CAD-plot PDFs), verifying ВОР against project documentation, or deciding between text layer / find_tables / vision parsing.
---

# Парсинг PDF РД: текстовый слой → спецификации → vision

Итоги разбора «Санпропускник» (2026-09-08, отчёт:
`e:\Проекты Симрус\К1\К1 Архив\Kaluga K1\11 Работа\2026\Санпропусник\ОТЧЁТ_AI-парсеры_PDF_DWG_vs_наши_инструменты_2026-09-08.md`).

## Главный факт

**Текстовый слой CAD-PDF полон**: у 8/9 комплектов РД 12 781–311 300 симв./PDF, ВСЕ количества
спецификаций присутствуют обычным текстом, даже там, где `find_tables` возвращает 0 полезных строк.
`find_tables` покрывает ~35% спек-строк; anchor-парсер по тексту — ~90%+. Vision нужен только для
сканов, повёрнутых листов и рукописных правок.

## Порядок действий (decision tree)

1. **Текстовый слой**: `designbase ingest-pdf-texts --building X --path <pdf>…`
   (модуль `ingest/pdf_texts.py`: плоский get_text постранично → kind=pdf_text;
   страницы < 50 симв. пропускаются — растр/пустые). Цена: секунды.
2. **Anchor-парсер спек**: `designbase extract-pdf-spec --building X --path <pdf>…
   [--pages 13-17,24] [--dry-run]` (модуль `ingest/pdf_spec_text.py` → kind=pdf_spec).
   Ключевое: get_text() отдаёт спеки CAD-печати ПОКОЛОНОЧНО → парсер группирует
   слова в y-полосы (page_bands) и берёт пары «единица количество» как якоря позиций:
   одиночные единицы («мп», «комплект») + голое число = та же пара; титул-блок
   и продолжение шапки — границы; разделы подтверждаются контекстом
   (дальше pos-полоса или пара с именным заходом). Голые числа без единицы —
   НЕ позиции (массы/номера страниц). Покрытие на Санпропускнике (2026-09-09):
   640 позиций — ВК 76, ОВ 343, ТС 34 (вкл. ДК-таблицу 11 поз.), ТХ 79, ЭМ 54,
   ПС 15, СС 39. Все эталонные факты (ППМИ 66, задвижка Ду50 2, ДПУ-М 20,
   ДК 100х150 10, ZVG 2, кран Ду15 19) находятся с цитатой и страницей.
3. **find_tables** — только для ruled-таблиц (ПС/СС/ЭМ спеки: дополняет anchor).
4. **Vision точечно** (1–3 страницы, не весь PDF):
   `designbase pdf-page-image <pdf> --page 17 --dpi 300` → PNG в output/pdf_pages.
   Показано для: повёрнутых таблиц (дверная спека АР л.17), сканов
   (АС: 148 символов текста), рукописных правок.
5. **Ручное/человек** — последняя инстанция.

## Правила интерпретации (типовые ошибки ИИ и людей)

- **Scope страницы ≠ scope раздела**: спека на листе может быть на сегмент трассы (66 м), а ВОР — на весь
  участок (240 м). Сверяй диапазоны прежде чем объявлять расхождение (кейс ТС: стыки 12 vs 48).
- Число «из головы» — не факт: в кейсе «Санпропускник» chat-ИИ выдал UTP 2103/2200 м (в документе 1639,
  тройное совпадение журнал=спека=ВОР), ОК-1=25 (в тексте нет), двери 66 (его же таблица суммируется в 65).
- Каждое количество — с цитатой (`ЦИТАТА — дословная подстрока со страницы`) и номером страницы.
- Пересчёты ИИ (площади из экспликации, ±%) помечать как derived, не как факт.

## Проверка моделей (слепые прогоны, изолированная папка, цитаты обязательны)

- `opencode/big-pickle`: 9/9 в своей области, но режет область по буквальному прочтению промпта
  (перечисляй ВСЕ ожидаемые типы заголовков таблиц).
- `opencode/muse-spark-1.3`: 42/42, сам строит `get_pixmap` → vision-сверку, 279 с, аудит tool-calls чистый.
- Задание всегда: изолированная папка (вне Temp с чужими результатами), запрет чтения вовне,
  после прогона — аудит tool-calls по `opencode.db`.

## Целевые CLI DesignBase (готовы, 2026-09-09)

- `designbase ingest-pdf-texts <pdf> --building X` — постраничный дамп текста в чанки БД (provenance).
- `designbase extract-pdf-spec <pdf> --pages 13-17 --building X [--dry-run]` — anchor-парсер → kind=pdf_spec
  (таблица `| Поз | Наименование | Ед | Кол |`, metadata: title/header/page/items).
- `designbase pdf-page-image <pdf> --page 17 --dpi 300` — PNG для vision.
- Golden-набор (tests/test_pdf_spec_text.py::test_golden_sanprousknik_facts): ТС ППМИ 66/Ду50 2,
  ВК кран Ду15 19, ОВ/ТХ/ЭМ/ПС/СС непустые спеки; синтетика собирается через
  fitz.TextWriter + arial (insert_text кириллицу не держит).
- Известные ограничения v1: экспликации (ВК л.9–10) тоже парсятся как spec-кандидаты
  (фильтровать по title при строгой сверке); префиксы разделов/штампа иногда попадают
  в имя (raw хранит правду); «То же»-строки без pos (qty+unit+страница верны).

## Питфоллы

- Проверять спек-страницы по диапазону листов из ведомости, а не первые 5 страниц PDF.
- `Единица измерения` может отсутствовать (спеки ДК — только Кол-во/Масса).
- Количество в спеке может стоять на другой строке блока, чем наименование (multi-row) —
  брать `get_text("blocks")` с координатами и сопоставлять по y.
- В PowerShell: `$env:PYTHONIOENCODING='utf-8'`; PyMuPDF: `import pymupdf` (fitz deprecated).
